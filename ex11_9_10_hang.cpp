// created by pi on 11/13/20
// brief: record hang hao of string

#include <iostream>
#include <string>
#include <vector>
#include <list>
#include <map>
#include <algorithm>

using namespace std;

int main() {
	map<string, list<size_t>> m;

	map<vector<int>::iterator, int> mv;
	map<list<int>::iterator, int> ml;

	vetor<int> vi;
    mv.insert(pair<vector<int>::iterator, int>(vi.begin(), 0));

    list<int> li;
    mi.insert(pair<vector<int>::iterator, int>(li.begin(), 0));

    return 0;
}